import json
import re
import requests
import csv
import time
import random

print('\r注入结束', end="", flush=True)
print('\r注入结束1', end="", flush=True)
print('\r注入结束2', end="", flush=True)
print('\r注入结束3', end="", flush=True)
print('\r注入结束4', end="", flush=True)